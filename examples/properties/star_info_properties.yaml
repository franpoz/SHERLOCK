TARGETS:
  TIC 273369281:
    SECTORS: "all"
    # Defining star properties for TIC 273369281 executions.
    STAR:
      MASS: 1.20
      MASS_LOWER_ERROR: 0.10
      MASS_UPPER_ERROR: 0.30
      RADIUS: 1.15
      RADIUS_LOWER_ERROR: 0.05
      RADIUS_UPPER_ERROR: 0.25
      TEFF: 4000
      LUM: 1.25
      #LOGG: 0.15
      LD_COEFFICIENTS: [0.25, 0.64]
#Instead of run several independent runs of sherlock, the user may add more stars over the same .yaml file to other targets
